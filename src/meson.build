# libraries that other subsystems might depend uppon first, in their
# respective dependency order

subdir('port')
subdir('common')
subdir('interfaces/libpq')
subdir('fe_utils')

subdir('timezone')

subdir('backend')
subdir('backend/jit/llvm')

subdir('bin')

subdir('pl')
